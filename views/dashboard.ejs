<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("partials/meta.ejs")  %> 
    <title>RecipEasy: My Recipes</title>
</head>
<body>
    <div class="container">
        <%- include('partials/header-nav.ejs') %> 
        <main>
            <div class="dashboard">
                <h2>Dashboard</h2>
                <h3> My Recipes </h3>
                <div class="recipe-grid">
                <% for (var recipe of recipes) {
                %>
                    <div class="recipe-card">
                        <div class="recipe-img-details">
                            <img src="/uploads/<%= recipe.rPhotoName%>" alt="recipe photo">
                            <div>
                            <p><b> <%= recipe.rRecipeName %>  </b></p>
                            <p>Made By: <%= recipe.rName %> <br>
                                <a href="/print/<%= recipe._id %>"> <b>View</b> </a> -- 
                                <a href="/edit-recipe/<%= recipe._id %>"> <b>Edit</b> </a> -- 
                                <a href="/delete-recipe/<%= recipe._id %>"> <b>Delete</b> </a>
                                </p>
                            </div>
                        </div>
                        <div class="recipe-card-hover">
                        <p>Ingredients:</p>
                        </div>
                    </div>
                <%
                } %> 
                </div>
            </div>
        </main>
        <%- include("partials/footer.ejs")  %> 
    </div>
</body>
</html>